class PurchaseItemModel{
  String name;
  String description;
  num amount;
  int quantity;
  String currency;

  PurchaseItemModel(this.name, this.description, this.amount, this.quantity, this.currency);

}